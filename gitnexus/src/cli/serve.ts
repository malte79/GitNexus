/**
 * Serve Command
 *
 * Epic 04 only establishes the final command shape.
 * The actual repo-local HTTP service lifecycle lands in Epic 05.
 */

export const serveCommand = async () => {
  console.error('CodeNexus serve is not implemented yet.');
  console.error('Epic 05 will add the repo-local HTTP service lifecycle behind `codenexus serve`.');
  process.exitCode = 1;
};
