Title: Rsproxy Full Endorsement
Assigned to: Agent 1
Lane: Bridge
Status: doing
Objective: Close the final endorsement blockers in the March 13, 2026 `rsproxy` product report so CodeNexus becomes good enough on `/Users/alex/Projects/roblox/rsproxy-agent-1` to be credibly endorsed as the primary structural analysis tool for repo-wide refactor planning. This epic targets production-owner discovery, first-class Lua and returned-table module modeling, stronger container-level impact, subsystem-oriented summary, and actionable confidence semantics. Success is not “better than before”; success is a preserved before-versus-after record that supports the user’s full endorsement standard on the same `rsproxy` checkout state.
In scope: reproduce the March 13, 2026 `rsproxy` report against `/Users/alex/Projects/roblox/rsproxy-agent-1` at commit `adcfb3302d21da0358716ba43b93eb5df326a7af` as the new acceptance baseline; improve `query` so broad subsystem discovery ranks production owners above tests by default; add an explicit owner-biased discovery mode that surfaces subsystem owners and central containers first; strengthen Lua and returned-table module or container modeling so Roblox and Luau ownership units are first-class in `context` and `impact`; improve container-level `impact` propagation and add actionable confidence breakdowns instead of a single opaque `partial` label; add a subsystem-oriented `summary` mode that exposes subsystem owners, production-versus-test distribution, and dependency pressure; define Roblox and Luau benchmark cases as first-class acceptance evidence for these behaviors; keep `codenexus help`, CLI docs, and architecture docs aligned with the shipped product surface; prove the result with a preserved complaint matrix and explicit endorsement-oriented acceptance evidence on the same unchanged `rsproxy` checkout state.
Out of scope: new transport or runtime architecture; direct-backend bypass of the repo-local HTTP service; embeddings or any new semantic retrieval engine; branch-specific indexing or query behavior; live Roblox game, Studio, or gameplay integration in `rsproxy`; speculative graph features unrelated to the named blockers; compatibility aliases for removed legacy CLI surfaces; cloud-hosted benchmark infrastructure.
Dependencies: [/Users/alex/Projects/GitNexusFork-agent-1/planning/master-intent.md](/Users/alex/Projects/GitNexusFork-agent-1/planning/master-intent.md); [/Users/alex/Projects/GitNexusFork-agent-1/docs/cli/commands.md](/Users/alex/Projects/GitNexusFork-agent-1/docs/cli/commands.md); [/Users/alex/Projects/GitNexusFork-agent-1/docs/architecture/mcp-http-runtime.md](/Users/alex/Projects/GitNexusFork-agent-1/docs/architecture/mcp-http-runtime.md); [/Users/alex/Projects/GitNexusFork-agent-1/docs/architecture/repo-local-implementation.md](/Users/alex/Projects/GitNexusFork-agent-1/docs/architecture/repo-local-implementation.md); [/Users/alex/Projects/GitNexusFork-agent-1/docs/architecture/repo-state-model.md](/Users/alex/Projects/GitNexusFork-agent-1/docs/architecture/repo-state-model.md); [/Users/alex/Projects/GitNexusFork-agent-1/planning/epics/done/14-rsproxy-structural-analysis-follow-through.md](/Users/alex/Projects/GitNexusFork-agent-1/planning/epics/done/14-rsproxy-structural-analysis-follow-through.md); current structural surfaces in [/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/cli/index.ts](/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/cli/index.ts), [/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/cli/info.ts](/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/cli/info.ts), [/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/cli/query.ts](/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/cli/query.ts), [/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/cli/context.ts](/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/cli/context.ts), [/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/cli/impact.ts](/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/cli/impact.ts), [/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/cli/summary.ts](/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/cli/summary.ts), [/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/mcp/local/local-backend.ts](/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/mcp/local/local-backend.ts), [/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/mcp/resources.ts](/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/mcp/resources.ts), [/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/mcp/tools.ts](/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/mcp/tools.ts), and Lua ingestion surfaces under [/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/core/ingestion/](/Users/alex/Projects/GitNexusFork-agent-1/gitnexus/src/core/ingestion/); the exact March 13 command set from the report remaining the canonical acceptance corpus for this epic; the `rsproxy` source checkout remaining unchanged while the epic is implemented so the March 13 baseline stays comparable; the repo-local benchmark evidence living in this repo rather than external systems.
Risks: owner-biased query work may over-correct and bury genuinely useful tests or secondary operational anchors; a new owner mode may become a vague wrapper over existing query behavior unless acceptance is tied to the exact `rsproxy` prompts from the report and the baseline records top-owner positions explicitly; default broad-query behavior may remain too noisy if the epic only makes `--owners` look good while leaving the normal path under-tuned; Lua returned-table modeling may over-attribute same-file helpers or internal locals as exported members if grounding is not explicit; container-level impact may become broader but less trustworthy if propagation outruns graph quality; confidence breakdowns may add words without making decisions more actionable unless each sub-signal is machine-readable, consistently ordered, and tied to concrete evidence surfaces; subsystem summary may sprawl into a second analytics product instead of a bounded structural overview if it is not tied to deterministic derivation, stable ordering, and explicit reuse of existing indexed facts rather than new hidden analytics state; subsystem naming may become hand-wavy if labels are not grounded in the same graph facts used to rank owners; Roblox and Luau benchmark coverage may devolve into ad hoc examples unless the benchmark set is locked and rerun on every closeout; before-versus-after proof may become invalid if the `rsproxy` checkout changes and the team quietly compares against a new state; endorsement claims may drift into prose unless the epic enforces a complaint matrix plus a final explicit endorsement check.
Rollback strategy: keep the existing repo-local HTTP service and top-level CLI shape intact; if the `rsproxy` checkout changes during implementation, stop and explicitly rebaseline rather than quietly comparing against a new repo state; if owner-biased ranking regresses exact lookup, explicit test-seeking prompts, or default broad-query usefulness, revert only the owner-ranking changes; if Lua or container modeling becomes noisy, remove the non-grounded expansion and keep explicit weak-wrapper or partial-confidence reporting rather than shipping fabricated ownership; if container-level impact propagation becomes inflated, narrow it back to grounded members and degrade confidence rather than overstating blast radius; if subsystem summary becomes noisy, unstable between runs, redundant with the existing summary surface, or dependent on new hidden analytics state, keep the existing `summary` surface and remove only the subsystem-oriented expansion; if confidence breakdowns are not machine-readable or decision-useful, trim them back to a smaller truthful set instead of keeping decorative sub-signals; if benchmark work becomes detached from product behavior, keep the locked benchmark corpus and remove any extra scaffolding rather than preserving a pseudo-framework.

### [ ] 101 Capture The March 13 `rsproxy` Endorsement Baseline

Surface: `/Users/alex/Projects/roblox/rsproxy-agent-1`; current `codenexus` CLI and repo-local MCP surfaces; epic-local evidence
Work: Reproduce the March 13, 2026 `rsproxy` report exactly enough to preserve a stable before-state baseline for every remaining endorsement blocker. This baseline must run against the `rsproxy` source checkout through CodeNexus only, not through any live Roblox runtime or Studio session. It must use the exact March 13 command set from the report as the canonical acceptance corpus, with no substitutions or prompt rewrites. It must capture the broad subsystem `query` prompts, the central Python and Lua `context` cases, the central-owner and Lua-owner `impact` cases, the current `summary` output, and the user’s endorsement standard itself. The exact commands, observed weak outputs, repo commit tested, and repo-state assumption must be preserved in an epic-local evidence section or linked artifacts so later closeouts compare against a fixed source of truth rather than memory. The evidence must include a stable complaint-by-complaint matrix keyed to the report items and an explicit “endorsement result” row that starts as not yet earned. That matrix must also preserve structured baseline fields for later comparison: top production-owner positions for each broad query, module member counts for the Lua container cases, whether container relationships are present, whether affected modules and processes are present for the impact cases, and the exact confidence shape shown to the user.
Tests: real product baseline run against `/Users/alex/Projects/roblox/rsproxy-agent-1` through `codenexus` only; preserved command transcript or linked artifacts; explicit complaint matrix keyed to production-owner ranking, Lua module modeling, container impact, subsystem summary, confidence semantics, and endorsement outcome; preserved structured baseline fields for ranking position, member counts, relationship presence, affected module or process presence, and confidence shape; verification that the captured baseline uses the exact March 13 command set as the canonical corpus
Docs: None

### [ ] 102 Add Production-Owner Discovery And Owner-Biased Query Mode

Surface: `gitnexus/src/mcp/local/local-backend.ts`; `gitnexus/src/mcp/tools.ts`; `gitnexus/src/cli/query.ts`; `gitnexus/src/cli/info.ts`; `docs/cli/commands.md`; `docs/architecture/repo-local-implementation.md`
Work: Improve broad subsystem discovery so production owners dominate broad prompts by default, and add an explicit owner-biased mode for users who want the main subsystem owners rather than a blended relevance list. The shipped surface should be `codenexus query ... --owners` unless implementation proves that the existing query surface cannot express owner mode cleanly; this epic should not introduce a second top-level discovery command without a documented product reason. Ranking must strongly demote tests and secondary wrappers for broad discovery prompts unless the user explicitly asks for tests. The owner-biased path must prefer central production containers, runtime owners, entrypoints, and subsystem anchors over test harnesses and peripheral definitions. This unit is not complete if only `--owners` improves while the default broad-query path remains materially noisy. Done when the report’s broad prompts consistently show bridge runtime, routes, services, plugin runtime, and shell owners before tests in the default path, and the owner-biased mode is clearly distinct from the generic “find anything related” path by surfacing a shorter, owner-heavier list.
Tests: unit tests for owner-biased ranking and explicit test-seeking inversion; integration tests for the exact `rsproxy` prompts from the March 13 report; regression tests protecting exact lookup and non-owner exploratory query behavior; before-versus-after evidence against the `101` baseline prompts, including recorded top-owner positions for default query and owner mode separately; CLI tests verifying the shipped owner mode stays on the existing query surface unless an explicit documented exception is taken
Docs: `docs/cli/commands.md`; `docs/architecture/repo-local-implementation.md`

### [ ] 103 Make Lua Returned-Table Modules First-Class Containers

Surface: `gitnexus/src/core/ingestion/luau-module-symbols.ts`; `gitnexus/src/core/ingestion/parsing-processor.ts`; `gitnexus/src/core/ingestion/workers/parse-worker.ts`; `gitnexus/src/mcp/local/local-backend.ts`; `docs/architecture/repo-local-implementation.md`
Work: Strengthen Lua and Luau returned-table module modeling so structurally clear returned-module patterns are treated as first-class containers rather than weak wrappers. This work must ground exported module members even when they are assembled through a local table and returned later, connect internal methods to the exported module symbol when the export pattern is structurally clear, and preserve explicit confidence when the structure is truly ambiguous. It must not be satisfied by broad same-file ownership guesses. Done when `runtime_manager`-style modules expose a meaningful container view with multiple grounded members, incoming and outgoing relationships that reflect the actual exported module surface, and a container explanation that describes real ownership rather than apologizing for weak coverage.
Tests: unit tests for returned-table module extraction, delegate recovery, and exported-member grounding; integration tests for Lua and Luau repos with returned-table patterns; real `rsproxy` acceptance for `runtime_manager` and `TransportClient`; regressions that internal same-file locals are not over-attributed as exported module members
Docs: `docs/architecture/repo-local-implementation.md`

### [ ] 190 Milestone 100 Closeout

Surface: baseline evidence; owner discovery; Lua container modeling; docs and help surfaces
Work: Prove the March 13 endorsement baseline is preserved and the first two endorsement blockers are materially improved against it. Closeout is complete only when the complaint matrix shows measurable improvement on production-owner discovery and Lua module modeling on the same unchanged `rsproxy` checkout state, the owner-biased query mode is documented in `codenexus help`, and the evidence is strong enough that the user would no longer describe `query` as “too blended” or `runtime_manager` as “just one weak exported member.” The closeout evidence must compare the recorded baseline fields directly rather than replacing them with prose.
Tests: targeted unit and integration coverage for units `101` through `103`; before-versus-after comparison against the `101` baseline for broad query and Lua module cases, including recorded top-owner positions and Lua member-count or relationship deltas; `npm run build --prefix gitnexus`
Docs: None

### [ ] 201 Strengthen Container-Level Impact And Add Actionable Confidence Breakdown

Surface: `gitnexus/src/mcp/local/local-backend.ts`; `gitnexus/src/mcp/tools.ts`; `gitnexus/src/cli/impact.ts`; `gitnexus/src/cli/context.ts`; `gitnexus/src/cli/info.ts`; `docs/cli/commands.md`; `docs/architecture/repo-local-implementation.md`
Work: Improve `impact` for large composite and container symbols so it becomes good enough for refactor planning on the hottest symbols in `rsproxy`. This work must strengthen container-to-member expansion where grounded, improve propagation from impacted members to affected modules, areas, and processes where the graph supports it, and replace the single opaque `partial` label with actionable confidence sub-signals. At minimum, confidence should separately communicate member coverage, incoming-edge coverage, outgoing-edge coverage, and higher-level propagation coverage. Those sub-signals must be machine-readable and consistently ordered so tests and acceptance evidence can compare them directly. The output must tell users which parts are strong enough to trust and which remain weak. Done when central symbols such as `CommandBridgeHandler`, `ProtocolRouter`, and `WorldProjectionRuntime` expose believable architectural impact and confidence semantics that support planning decisions rather than merely disclaiming uncertainty.
Tests: unit tests for container-level impact propagation and confidence sub-signals; integration tests for the central-owner `impact` cases from the March 13 report; regressions that same-file or file-co-location backfill is not reintroduced; before-versus-after evidence against the `101` baseline for central-owner impact, including presence of affected modules or processes and the exact confidence shape
Docs: `docs/cli/commands.md`; `docs/architecture/repo-local-implementation.md`

### [ ] 202 Add A Subsystem-Oriented Summary Mode

Surface: `gitnexus/src/mcp/local/local-backend.ts`; `gitnexus/src/mcp/tools.ts`; `gitnexus/src/cli/summary.ts`; `gitnexus/src/cli/info.ts`; `docs/cli/commands.md`; `docs/architecture/repo-local-implementation.md`
Work: Extend `summary` with a subsystem-oriented architectural view such as `codenexus summary --subsystems` or a clearly better equivalent within the current CLI shape. The subsystem view must show subsystem or component name, top owning symbols or files, approximate fan-in or fan-out pressure, production-versus-test split inside that subsystem, and the hottest anchors or central owners. It must remain read-only and derived from existing indexed facts rather than adding a second analytics subsystem. Subsystem rows and labels must be grounded in existing graph facts, owner-ranking signals, or community/process metadata already present in the index; if those facts are too weak for a stable subsystem, the implementation should emit fewer, more truthful subsystem rows rather than inventing labels. The derivation and ordering must be deterministic enough that repeated runs on the same index do not reshuffle the main subsystem list without underlying graph changes. This unit is not complete if the subsystem summary only looks plausible in prose but does not materially reduce the number of follow-up queries needed for the `rsproxy` refactor-list workflow described in the report. Done when a user can produce a credible first-pass refactor list from one subsystem summary plus a handful of follow-up calls rather than stitching together many mixed manual queries.
Tests: unit tests for subsystem-summary formatting, derivation, grounded labeling, and stable ordering; integration tests for subsystem summary on representative repos; real `rsproxy` acceptance showing subsystem owners, pressure, and production-versus-test split; regression tests that the existing `summary` surface remains available and truthful; acceptance evidence that the subsystem summary materially compresses the follow-up query burden for the `rsproxy` workflow
Docs: `docs/cli/commands.md`; `docs/architecture/repo-local-implementation.md`

### [ ] 290 Milestone 200 Closeout

Surface: impact behavior; confidence semantics; subsystem summary; real acceptance evidence
Work: Prove the remaining endorsement blockers are materially improved against the preserved March 13 baseline. Closeout is complete only when `impact` on the hottest central symbols is strong enough to support refactor planning, the new confidence semantics are actionable rather than decorative, and the subsystem summary view is useful enough that the user no longer needs to infer architectural structure manually from clusters and symbols alone. The closeout evidence must compare the recorded baseline fields directly and show that the confidence breakdowns improved decision usefulness rather than merely adding more output.
Tests: targeted unit and integration coverage for units `201` and `202`; before-versus-after comparison against the `101` baseline for impact and summary, including confidence-shape comparison and subsystem-output stability checks; `npm run build --prefix gitnexus`
Docs: None

### [ ] 301 Make Roblox And Luau A Locked Structural Benchmark

Surface: `gitnexus/test/integration/`; `gitnexus/test/unit/`; benchmark fixtures or reference cases under `gitnexus/test/`; `docs/architecture/repo-local-implementation.md`; epic-local evidence
Work: Create a locked benchmark set for Roblox and Luau structural analysis quality so the endorsement bar is measured continuously rather than rediscovered manually. The benchmark must cover returned-table modules, module wrappers, method tables, runtime orchestrators, owner ranking, context completeness, and impact accuracy. It must include the `rsproxy` complaint cases and at least one additional repo-local Luau shape that exercises returned-module patterns without Python. The benchmark should remain repo-local and test-oriented, not a new product feature. The benchmark corpus and expected assertions must be fixed in-repo so the suite cannot drift by informal fixture edits. At minimum the locked cases must cover: broad owner-biased ranking, a returned-table module with multiple grounded exported members, a weak-wrapper module that should remain partial rather than overclaimed, and a central composite symbol whose impact propagation is intentionally stronger than file-area hints but still bounded by grounded evidence. Done when the main Roblox and Luau endorsement cases can be rerun as a first-class validation surface and the product docs clearly treat Luau support as a benchmarked quality target rather than a caveat.
Tests: locked benchmark integration tests and targeted unit tests for benchmark fixtures; regression runs covering owner ranking, module completeness, weak-wrapper honesty, and impact accuracy; explicit acceptance evidence showing the benchmark suite is part of the normal validation story; tests that the benchmark corpus and expected assertions are loaded from fixed in-repo fixtures
Docs: `docs/architecture/repo-local-implementation.md`

### [ ] 390 Milestone 300 Closeout

Surface: benchmark suite; validation surfaces; docs
Work: Prove the Roblox and Luau benchmark is real, locked, and relevant to the product behaviors the endorsement depends on. Closeout is complete only when the benchmark suite runs against the implemented behaviors, catches the intended failure modes, and is referenced in docs as a maintained quality bar for Roblox and Luau support. The closeout evidence must identify the exact benchmark fixtures exercised so the benchmark claim is auditable later.
Tests: benchmark test runs and targeted regressions for the locked Roblox or Luau cases; explicit evidence naming the exercised benchmark fixtures; `npm run test:integration --prefix gitnexus`
Docs: None

### [ ] 401 Run Final `rsproxy` Endorsement Acceptance

Surface: `/Users/alex/Projects/roblox/rsproxy-agent-1`; top-level `codenexus` structural commands; epic-local evidence
Work: Rerun the full March 13 command set from the endorsement report against `rsproxy` through the shipped CLI and compare it directly to the preserved `101` baseline. The acceptance pass must use the same unchanged `rsproxy` checkout state captured in the baseline unless the user explicitly approves rebaselining. It must use the same canonical command corpus recorded in `101`, plus the new owner-biased query mode and the new subsystem summary mode. It must cover `summary`, the new subsystem summary mode, the broad subsystem `query` prompts, the owner-biased query mode, the Python and Lua `context` calls, the central-owner and Lua-owner `impact` calls, and the confidence semantics shown on those outputs. Record what is now materially better and whether the user’s endorsement standard is now actually met. The final evidence must preserve the exact commands used, update the `101` complaint matrix with after-state entries, and include an explicit endorsement verdict row whose acceptable values are `not yet earned` or `earned`. That verdict cannot be supported by prose alone: the after-state matrix must include the same structured fields captured in `101`, and the verdict must be justified against them item by item. The verdict is not earned if the matrix still requires the user to explain away noisy broad-owner discovery, weak Lua container ownership, or non-actionable confidence output. Done only when the evidence supports `earned`.
Tests: real product acceptance run against `/Users/alex/Projects/roblox/rsproxy-agent-1` through `codenexus` only; explicit before-versus-after comparison for every blocker in the March 13 report; preserved final command transcript or linked artifact paths; verification that the compared run used the same checkout state recorded in `101` unless the user explicitly approved rebaselining; explicit endorsement verdict in the evidence; verification that the after-state matrix preserves the same structured comparison fields as `101`; verification that the same canonical March 13 command corpus was used plus only the new owner-mode and subsystem-summary commands; explicit check that the verdict would still be `not yet earned` if any of the three primary blocker groups remain unresolved
Docs: None

### [ ] 490 Epic Closeout

Surface: query ranking; owner mode; Lua module modeling; impact trust; confidence semantics; subsystem summary; Roblox and Luau benchmark; final evidence
Work: Epic closeout is complete only when the remaining endorsement blockers from the March 13 `rsproxy` report are resolved strongly enough that a full endorsement is honestly supportable. The final record must show that production-owner discovery now ranks owners first, returned-table Lua modules are treated as meaningful containers, central-symbol blast radius is good enough for planning, subsystem summary reduces manual architectural inference substantially, confidence semantics explain what is trustworthy, and Roblox or Luau structural quality is benchmarked as a first-class target. Closeout requires the preserved complaint matrix to be fully updated with after-state status for each blocker and the endorsement verdict row to be `earned`. The matrix must remain auditable complaint by complaint and field by field so the endorsement claim is mechanically reviewable rather than interpretive.
Tests: `npm test --prefix gitnexus`; `npm run test:integration --prefix gitnexus`; `npm run build --prefix gitnexus`; `npm run lint:docs --prefix gitnexus`; `npm run check:docs-contracts --prefix gitnexus`; real acceptance pass against `/Users/alex/Projects/roblox/rsproxy-agent-1` through `codenexus`; explicit before-versus-after comparison against the `101` baseline on the same checkout state; verification that the complaint matrix is fully updated, field-compatible with the baseline, and the endorsement verdict is `earned`
Docs: None
